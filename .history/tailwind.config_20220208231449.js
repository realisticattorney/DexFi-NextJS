module.exports = {
  content: [
    "./pages/**/*.{js,ts,jsx,tsx}",
    "./components/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
//ipfs-http-client is a decentralized way of hosting our URI (nft images/files) using promises to hook up to IPFS so that they'll be hosting their imges on with IPFS (IPFS is a distributed peer-to-peer hypermedia protocol).
//hardhat can also initialize our solidity contracts
